<%= form_for @book do |f| %>
  <% if @book.errors.any? %>
      <div id="error_explanation">
      <h2><%= pluralize(@book.errors.count, "error") %> prohibited
      this books from being saved:</h2>
      <ul>
      <% @book.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
      <%= form_for @book, :url => books_path, :html => { :multipart => true } do |form| %>
         <%= form.file_field :cover %>
      <% end %>
    </div>
  <% end %>
  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>
  <p>
    <%= f.label :author %><br>
    <%= text_field_tag 'book[author]', @book.author.try(:name) %>
  </p>
  <p>
    <%= f.label :description%><br>
    <%= f.text_area :description %>
  </p>
  <p>

  </p>
  <p>
    <%= f.label :year%><br>
    <%= f.number_field :year %>
  </p>
    <%= f.file_field :cover %>
  <p>
    <%= f.submit %>
  </p>
<% end %>